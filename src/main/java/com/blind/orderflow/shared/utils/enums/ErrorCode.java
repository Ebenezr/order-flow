package com.blind.orderflow.shared.utils.enums;

public enum ErrorCode {

    NOT_FOUND,
    VALIDATION_ERROR,
    PAYMENT_FAILED,
    INVENTORY_UNAVAILABLE,
    INVALID_ORDER_STATE,
    ACCESS_DENIED,
    INTERNAL_ERROR
}